services:
  word-gpt-plus:
    build: .
    container_name: word-gpt-plus
    ports:
      - "3232:80"
      - "3100:3100"
    environment:
      - NODE_ENV=production
    volumes:
      - ./word-gpt-plus-logs:/app/data/logs
